module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  testMatch: ['**/__tests__/**/*.test.ts'], // Matches all .test.ts files in the __tests__ folder
  moduleFileExtensions: ['ts', 'tsx', 'js', 'json', 'node'],
};
